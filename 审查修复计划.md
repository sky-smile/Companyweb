## 审查修复计划 — 共 35 项任务

### 第一阶段：致命问题（阻止部署） — 4 项

| # | 任务 | 依赖 |
|---|------|------|
| #1 | 修复 Dockerfile COPY --chown 用户名/组名错误 | — |
| #3 | 修复 Nginx 配置结构和指令错误 | — |
| #2 | 修复 NODE_ENV vs SERVER_NODE_ENV 环境变量不一致 | — |
| #4 | 移除数据库 fallback/mock 数据系统 | — |

### 第二阶段：安全加固 — 8 项 ✅ 已完成

| # | 任务 | 依赖 |
|---|------|------|
| #7 | 添加 Helmet 安全头中间件 | — |
| #6 | 登录接口添加速率限制 | — |
| #5 | 移除 docker-compose 中硬编码的默认密钥/密码 | — |
| #9 | 权限校验改为实时数据库查询 | — |
| #12 | 富文本编辑器添加 XSS 防护 | — |
| #31 | 实现 Token 撤销机制 | — |
| #8 | JWT Token 存储改为 httpOnly cookie | 需 #31 |
| #25 | 增强 DTO 验证规则 | — |

### 第三阶段：性能与架构 — 6 项 ✅ 已完成

| # | 任务 | 依赖 |
|---|------|------|
| #10 | 前端详情页改为 SSR (Server Component) | — |
| #11 | 列表接口改为数据库端分页（skip/take） | 需 #4 |
| #14 | 使用 next/image 替代原生 img 标签 | — |
| #15 | 多步写入操作添加事务支持 | 需 #4 |
| #13 | 健康检查端点添加数据库连通性验证 | — |
| #33 | 前端性能优化（防抖、并行请求、dynamic import） | — |

### 第四阶段：基础设施 — 7 项

| # | 任务 | 依赖 |
|---|------|------|
| #20 | 添加 .dockerignore 文件 | — |
| #23 | 修复 Docker Compose 健康检查命令 | — |
| #24 | 修复 MariaDB 配置中的已废弃变量 | — |
| #26 | 锁定 Docker 镜像版本标签 | — |
| #27 | 修复 docker-compose 服务依赖和资源限制 | — |
| #28 | 消除硬编码 URL，统一使用环境变量 | — |
| #29 | 修复上传模块错误处理和文件清理 | — |

### 第五阶段：代码质量 — 10 项

| # | 任务 | 依赖 |
|---|------|------|
| #16 | 修复前端组件 any 类型滥用 | — |
| #18 | 修复后端 any 类型滥用 | — |
| #17 | 抽取管理员列表页通用 CrudPage 组件 | — |
| #30 | 消除后端重复的 DTO 和分页逻辑 | 需 #4 |
| #19 | 消除前端 CSS/组件重复并利用已有组件 | — |
| #21 | 管理员页面添加错误处理和用户反馈 | — |
| #22 | 完善 sitemap 和 SEO Metadata | — |
| #32 | 清理调试代码和内联样式 | — |
| #34 | 修复管理员面板 UX 缺陷 | — |
| #35 | 修正文档中的错误引用和补充遗漏信息 | — |

---

**建议执行顺序**：第一阶段 → 第二阶段 → 第三阶段 → 第四阶段 → 第五阶段。同一阶段内依赖少的任务可以并行处理。

## 第一阶段完成

8 个文件修改，122 行新增，477 行删除。已推送到 GitHub。

### 变更摘要

| 文件 | 变更 |
|------|------|
| `frontend/Dockerfile` | COPY --chown 组名 `nodejs` → `nextjs`（用户是 `nextjs`，组也应是 `nextjs`）|
| `server/Dockerfile` | COPY 添加 `--chown=nestjs:nodejs` |
| `docker-compose.yml` | 添加 `SERVER_NODE_ENV: production` |
| `docker/nginx/docker-compose.conf` | 移除 `http`/`events`/`worker_processes` 外层包装（此文件被 include 在 http 块内）；移除 `more_clear_headers`（需额外模块）；移除废弃的 `X-XSS-Protection` |
| `server/.../auth.repository.ts` | **移除 mock admin 后门** — 不再在 DB 不可用时返回硬编码密码的超级管理员 |
| `server/.../admin-user.repository.ts` | 移除 `isDatabaseReady()` + `getFallbackAdminUser()` |
| `server/.../news.repository.ts` | 移除 `isDatabaseReady()` 和所有 mock 数据返回 |
| `server/.../role.repository.ts` | 移除 `isDatabaseReady()` + `getFallbackRole()` |

总结：修复了 3 个致命部署阻塞问题 + 2 个关键安全漏洞（后门账号 + fallback 数据系统）。

## 第二阶段完成

26 个文件修改，228 行新增，24 行删除。提交 `557ffc1`。

### 变更摘要

| # | 任务 | 变更 |
|---|------|------|
| #5 | 移除 docker-compose 硬编码密钥 | `docker-compose.yml` 中的默认值改为 `:?` 强制必填语法 |
| #7 | Helmet 安全头中间件 | `main.ts` 添加 `helmet()` 中间件；新增依赖 `helmet@8` |
| #6 | 登录接口速率限制 | `app.module.ts` 添加 `ThrottlerModule`（全局 60次/分）；`auth.controller.ts` 登录端点限制 5次/分 |
| #25 | 增强 DTO 验证规则 | News/Announcement/Product/SitePage DTO 新增 `@MaxLength` 约束 |
| #12 | 富文本编辑器 XSS 防护 | 确认服务层 `sanitizeHtmlContent()` 已正确应用于所有内容存储路径 |
| #9 | 权限校验改为实时数据库查询 | `JwtStrategy.validate()` 改为每次请求查询数据库获取最新权限/角色/状态 |
| #31 | 实现 Token 撤销机制 | 新增 `admin_users.token_version` 字段；JWT payload 携带版本号；登录/改密/登出时递增版本号使旧令牌失效 |
| #8 | JWT 存储改为 httpOnly Cookie | 新增 `cookie-parser`；登录/刷新设置 httpOnly Cookie（sameSite=lax）；JwtStrategy 优先读 Cookie 再降级到 Authorization header；前端 `withCredentials: true`；登出调用 API 清除 Cookie |

## 第三阶段完成

6 个提交，19 个文件修改。

### 变更摘要

| # | 任务 | 提交 | 变更 |
|---|------|------|------|
| #13 | 健康检查 DB 验证 | `71ee28e` | `HealthService` 注入 `DataSource`，执行 `SELECT 1` 验证连通性，不可用时返回 `degraded` |
| #11 | 数据库端分页 | `13659ab` | 6 个列表端点从全量查询+内存分页改为 `QueryBuilder.skip().take().getManyAndCount()` |
| #15 | 事务支持 | `fddb5d5` | admin-user/role/site-content 多步写入包裹在 `DataSource.transaction()` 中 |
| #10 | SSR 详情页 | `1be9aa1` | 3 个详情页从 Client Component 转为 async Server Component，`notFound()` 处理 404，`generateMetadata` 支持 SEO |
| #14 | next/image | `c7392fe` | LazyImage/NewsCardImage/SiteHeader/SiteFooter 全部改用 `next/image`，自动 WebP+srcset |
| #33 | 性能优化 | `e2e94c5` | 新增 `useDebounce` hook；MobileMenu/BackToTop 使用 `next/dynamic` 动态导入 |
